using UnityEngine;

public class Stun : MonoBehaviour
{
   /* float until;
    public bool IsStunned => Time.time < until;
    public void Apply(float seconds) => until = Mathf.Max(until, Time.time + Mathf.Max(0, seconds));*/
}
